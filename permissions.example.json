{
  "trustedWorkspaces": [
    "/home/YOUR_USER/proyekku"
  ],
  "permissions": {
    "allow": [
      "read_file(/home/YOUR_USER/proyekku)",
      "write_file(/home/YOUR_USER/proyekku)",
      "command(pwd)",
      "command(ls)",
      "command(find)",
      "command(tree)",
      "command(cat)",
      "command(head)",
      "command(tail)",
      "command(grep)",
      "command(rg)",
      "command(file)",
      "command(stat)",
      "command(wc)",
      "command(which)",
      "command(whereis)",
      "command(git status)",
      "command(git diff)",
      "command(git log)",
      "command(git branch)",
      "command(git show)",
      "command(python)",
      "command(python3)",
      "command(pytest)",
      "command(node)",
      "command(npm)",
      "command(npx)",
      "command(php)",
      "command(composer)"
    ],
    "deny": [
      "command(sudo)",
      "command(su)",
      "command(shutdown)",
      "command(reboot)",
      "command(poweroff)",
      "command(halt)",
      "command(mkfs)",
      "command(fdisk)",
      "command(parted)",
      "command(dd)",
      "read_file(/home/YOUR_USER/.ssh)",
      "write_file(/home/YOUR_USER/.ssh)"
    ]
  }
}
